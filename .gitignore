# === Dependencies (install via npm on first run) ===
node_modules/

# === Runtime data (personal notes, schedules and generated indexes) ===
# Keep only code and deterministic test fixtures in version control.
.zhigui/
skill/.zhigui/

# === IDE / local tool configs (contain machine-specific paths) ===
.trae/
.cursor/
.mcp.json
.workbuddy/

# === Backups ===
backups/
.git.zhs-backup/

# === OS metadata ===
Thumbs.db
ehthumbs.db
.DS_Store

# === Local environment files (may contain secrets) ===
.env
.env.*

# === Backup / temp files ===
*.bak
*.tmp

# === Windows junk (reserved-name files from stray redirections) ===
nul
