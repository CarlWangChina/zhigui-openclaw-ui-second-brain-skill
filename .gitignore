# === Dependencies (included in repo for out-of-box use) ===
# node_modules/  — intentionally included so users don't need npm install

# === IDE / local tool configs (contain machine-specific paths) ===
.trae/
.cursor/
.mcp.json
.workbuddy/

# === Backups ===
backups/
.git.zhs-backup/

# === OS metadata ===
Thumbs.db
ehthumbs.db
.DS_Store

# === Local environment files (may contain secrets) ===
.env
.env.*

# === Backup / temp files ===
*.bak
*.tmp

# === Windows junk (reserved-name files from stray redirections) ===
nul

# === OS metadata ===
Thumbs.db
ehthumbs.db
.DS_Store

# === Local environment files (may contain secrets) ===
.env
.env.*
